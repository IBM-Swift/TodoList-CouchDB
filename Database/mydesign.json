{
  "_id": "_design/example",
  "views" : {
    "all_todos" : {
      "map" : "function(doc, ownerID) { 
        if (doc.type == 'todo' && doc.active && doc.user == ownerID) { 
          emit(doc._id, [doc.title, doc.completed, doc.order]); 
        }
      }"
    },
      "total_todos": {
        "map" : "function(doc) { 
          if (doc.type == 'todo' && doc.active) { 
            emit(doc.id, 1); 
          }
        }",
        "reduce" : "_count"
      }
  }
}
