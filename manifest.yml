declared-services:
  TodoListCloudantDatabase:
    label: cloudantNoSQLDB
    plan: Shared
applications:
- name: TodoListCloudantApp
  memory: 256M
  instances: 1
  random-route: true
  services:
  - TodoListCloudantDatabase

